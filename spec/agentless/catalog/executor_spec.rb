RSpec.describe Agentless::Catalog::Executor do
  it "has a version number" do
    expect(Agentless::Catalog::Executor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
